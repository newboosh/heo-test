# Heo-Test Plugin Configuration
# Copy this file to .env.local in your project and customize
# All variables are optional - only set what you need

# =============================================================================
# Origin Repository (PAT-authenticated git origin remote)
# =============================================================================
# The repository URL and PAT for git operations (push, PR creation, etc.)
# Used by: scripts/github-auth.sh, scripts/github-setup.sh
# REPO_ORIGIN_URL=https://github.com/username/repo.git
# REPO_ORIGIN_PAT=ghp_xxxxxxxxxxxxxxxxxxxx
#
# Legacy (still supported, but REPO_ORIGIN_PAT takes precedence):
# GITHUB_PAT=ghp_xxxxxxxxxxxxxxxxxxxx

# =============================================================================
# Production Deployment (for /deploy, /health-check, /logs commands)
# =============================================================================
# Required for production features:
# PRODUCTION_DOMAIN=your-domain.example.com
# PRODUCTION_SERVER_IP=123.45.67.89

# Optional (have sensible defaults):
# APP_NAME=my-app
# SSH_USER=root
# SSH_KEY_NAME=server_key
# COMPOSE_PROJECT=deployment
# DB_PATH=backend/database/app.db
# BACKUP_DIR=backups

# =============================================================================
# Server Paths (computed from above, but can be overridden)
# =============================================================================
# SERVER_PATH=/opt/${APP_NAME}
# DB_FULL_PATH=${SERVER_PATH}/${DB_PATH}
# BACKUP_FULL_PATH=${SERVER_PATH}/${BACKUP_DIR}
